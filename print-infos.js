const client = {
  name: `André`,
  age: 36,
  id: `12543652266`,
  email: `andre@email.com`
}

console.log(`I'm called ${client.name} and i'm ${client.age} years old`);
console.log(client.id.substring(0,3));